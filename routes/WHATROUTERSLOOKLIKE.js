const express = require("express");
const app = express.Router();

/*

app.customPath = WHATEVER PATH YOU WANT, instead of the file name
app.get("/THING", (req, res) => {
});

 */

module.exports = app;
